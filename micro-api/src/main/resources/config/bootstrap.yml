spring:
  application:
    name: api
  cloud:
    config:
      discovery:
        service-id: config
      enabled: true
      uri: http://localhost:9100/SudokuConfig/
      name: api
      profile: api_a